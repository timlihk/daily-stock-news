# Email Configuration
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_TO=recipient@email.com

# Optional: Custom SMTP Settings (leave blank to use Gmail)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false

# News API Configuration (Finnhub)
NEWS_API_KEY=your-finnhub-api-key

# Stock Configuration
STOCK_SYMBOLS=AAPL,GOOGL,MSFT,TSLA,AMZN

# Cron Schedule (default: 8 AM daily)
# Format: minute hour day month day-of-week
# IMPORTANT: Railway servers run in UTC timezone by default
# Example: 0 8 * * * = 8:00 AM UTC (adjust for your timezone)
# For PST (UTC-8): 0 16 * * * = 8:00 AM PST
# For EST (UTC-5): 0 13 * * * = 8:00 AM EST
CRON_SCHEDULE=0 8 * * *

# Timezone (optional, defaults to UTC on Railway)
# Set this to your timezone to have logs display in local time
# Examples: America/New_York, America/Los_Angeles, Europe/London
TZ=UTC

# Web Server Port (automatically set by hosting providers)
PORT=3000

# Redis Configuration (automatically set by Railway when you add Redis)
# REDIS_URL=redis://default:password@host:port